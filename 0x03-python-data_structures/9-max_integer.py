#!/usr/bin/python3
def max_integer(my_list=[]):
    num=len(my_list)
    if num == 0:
        return none
    else:
        max=my_list[0]
        for i in range(num):
            if max > my_list[i]:
                pass
            else:
                max=my_list[i]
    return max